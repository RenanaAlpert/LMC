#pragma once

#include <string>
#include <list>
#include <vector>

namespace experis
{

using Key = std::string;
using Val = size_t;
using MechinLanguage = size_t;
using Segment = size_t;
using Idxs = std::vector<size_t>;


}//experis
